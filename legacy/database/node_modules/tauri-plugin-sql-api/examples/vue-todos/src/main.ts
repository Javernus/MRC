import { createApp } from 'vue';
import { createPinia } from 'pinia';
import App from 'tauri-plugin-sql-api/examples/vue-todos/src/App.vue';
import 'virtual:windi.css';
import 'virtual:windi-devtools';

const app = createApp(App);
app.use(createPinia());
app.mount('#app');
